// 该文件专门用于创建整个应用的路由器
import VueRouter from "vue-router";
const router = new VueRouter({
    routes: [{
        path: '/about',
        component: About
    },
        routes: [{
            path: '/about',
            component: About
        },
    ]
})